# Shared styles

Target home for design tokens, shell layout primitives, reset rules, and cross-feature CSS contracts.

Large module CSS files should be split into feature-owned styles before moving rules here.
