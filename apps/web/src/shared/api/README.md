# Shared API helpers

Target home for the web HTTP client, common request options, and shared API error handling.

Feature repositories should keep module-specific query shapes and mapping logic inside `src/features/<module>`.
