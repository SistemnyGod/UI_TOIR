# Shared web layer

Shared code is allowed only when it is reusable across modules and has no feature-specific business rules.

Target subfolders:

- `ui` - UI primitives and layout components.
- `api` - shared HTTP and API error helpers.
- `styles` - tokens, app shell primitives, and cross-feature CSS contracts.
