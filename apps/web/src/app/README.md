# Web app layer

This folder is the target home for React bootstrap, app shell wiring, routing, and screen registry integration.

Do not put feature business logic here. Feature code belongs under `src/features/<module>`.
