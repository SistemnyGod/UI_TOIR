{
  "name": "@patrol360/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && vite build --configLoader runner",
    "preview": "vite preview",
    "verify": "npm run build",
    "test": "vitest --configLoader runner",
    "test:unit": "vitest run --configLoader runner",
    "test:unit:ci": "node -e \"require('node:fs').mkdirSync('../../TestResults/vitest',{recursive:true})\" && vitest run --configLoader runner --reporter=default --reporter=junit --outputFile.junit=../../TestResults/vitest/junit.xml",
    "test:structural": "node ../../tests/web/unit/run-unit-tests.mjs",
    "test:run": "npm run test:unit && npm run test:structural",
    "test:ci": "npm run test:unit:ci && npm run test:structural",
    "test:e2e": "node ./e2e/run-playwright.mjs"
  },
  "dependencies": {
    "lucide-react": "^0.561.0",
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.9.3",
    "vite": "^7.2.6",
    "vitest": "^4.1.6"
  }
}
