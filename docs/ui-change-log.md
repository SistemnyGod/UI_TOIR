# Журнал изменений интерфейсов

Этот документ хранит краткие записи о точечных изменениях UI, адаптивном поведении, исправлениях отображения и небольших улучшениях логики.

## Правила ведения

- Одна запись — одно законченное изменение или небольшой связанный набор правок.
- Для сложной правки указывается точечная проверка, а не полный регрессионный прогон.
- Для простой визуальной правки достаточно сборки или ручной проверки затронутого экрана.
- Отдельно фиксируются ограничения и задачи, сознательно оставленные на следующий этап.

## 2026-07-22 — базовый адаптивный каркас выдачи СИЗ

- Создание выдачи переведено на последовательный сценарий из четырёх шагов без копирования навигации.
- Добавлены реквизиты документа, раздельный подбор по норме, каталогу и наборам, состав и два режима предпросмотра.
- Для небольших ноутбуков и телефонов уплотнены панели, поля и действия.

Проверка: TypeScript, frontend build и проверка доступности приложения.

## 2026-07-22 — мобильный каркас общего интерфейса

- Исправлен конфликт поздних CSS-правил, сжимавший боковую навигацию в 72-px полосу.
- Разделы и подпункты сохранены читаемыми в горизонтально прокручиваемых панелях.
- Панель уведомлений ограничена viewport и больше не обрезается на узких экранах.

Проверка: CSS-каскад, frontend build, `git diff --check`.

## 2026-07-22 — мобильная модалка выбора номенклатуры СИЗ

- Двухколоночная область выбора на телефоне заменена вертикальной раскладкой.
- Фильтры, список, выбранные позиции и действия получили независимую безопасную прокрутку.
- Нижние действия остаются доступными на коротком viewport.

Проверка: CSS-каскад и frontend build.

## 2026-07-22 — safe-area мобильного рабочего пространства

- Добавлены safe-area отступы для верхней и нижней частей интерфейса.
- Действия модалок и форм не прижимаются к системной панели телефона.
- Изменения ограничены шириной до 640px.

Проверка: CSS-каскад и frontend build.

## 2026-07-22 — компактный shell небольших ноутбуков

- На ширине до 900px общий shell переведён в одну колонку.
- Рабочая область больше не теряет ширину под отдельную боковую колонку.
- Маршруты и desktop-раскладка выше 900px не изменены.

Проверка: CSS-каскад, frontend build, `git diff --check`.

## 2026-07-22 — адаптивный экран управления пользователями

- На ширине до 1180px панель прав переносится под список пользователей.
- Поздние legacy-правила больше не сжимают список и панель доступа.

Проверка: CSS-селекторы и frontend build.

## 2026-07-22 — safe-area мобильного редактирования аккаунта

- Нижняя панель действий формы учитывает safe-area и экранную клавиатуру.
- Поля не перекрываются закреплёнными действиями на коротком viewport.

Проверка: CSS-каскад и frontend build.

## 2026-07-23 — доступный календарь периода истории ЭМУ

- Поповер выбора периода получил ограничение высоты и собственную вертикальную прокрутку.
- Действия диапазона остаются доступны при открытой экранной клавиатуре.

Проверка: CSS-каскад, frontend build и health-check.

## 2026-07-23 — компактная пагинация PERCO на телефоне

- Кнопки назад/далее, номер страницы и размер страницы собраны в компактную строку.
- Счётчик записей переносится отдельно, длинные значения не создают переполнение.

Проверка: CSS-каскад, frontend build и health-check.

## 2026-07-23 — компактная панель видов истории ЭМУ

- Действия обновления и переключения вида собраны в устойчивую трёхколоночную панель.
- Общие мобильные правила больше не растягивают каждую кнопку на отдельную строку.

Проверка: CSS-каскад, frontend build и health-check.

## 2026-07-23 — базовый четырёхэтапный документ выдачи СИЗ

- Зафиксирован сценарий: сотрудник и документ → подбор СИЗ → состав и проверка → печать.
- Нормативное наименование отделено от конкретной номенклатуры.
- Дополнительная выдача из каталога создаёт отдельную строку.
- В рабочем составе не показываются размеры, цена, возврат и списание.
- Печатная бумажная структура остаётся в предпросмотре.

Проверка: frontend build, `git diff --check`, проверка новых файлов на U+FFFD.

## 2026-07-23 — завершение адаптивного мастера выдачи СИЗ

- Экран разделён на контейнер состояния и четыре компонента этапов.
- Нормативное и фактическое количество разделены.
- Добавлены реквизиты `issueType`, `responsibleName`, `basis`, сохранение и восстановление черновика.
- Категории норм сворачиваются; дополнительные строки удаляются отдельно от нормативов.
- Наборы показывают состав до применения, сопоставляют товары с нормами и не создают дубли.
- Финальная выдача сохраняется атомарным пакетным API с `expectedVersion`.
- На телефонах подбор и состав представлены карточками; бумажная таблица остаётся только в печати.
- Добавлены PDF/DOCX, предупреждения и блокировка сохранения при критических ошибках.

Проверка: точечные Vitest-тесты, frontend build, .NET build и визуальная проверка 1366×768, 1024×768, 390×844, 360×800.

## 2026-07-23 — единая визуальная система рабочего контура СИЗ

- Добавлен общий UI-слой на существующих переменных `--inventory-*`: спокойная палитра, единые радиусы, рамки, фокус и минимальные тени.
- Введён `PpeButton` с вариантами `primary`, `secondary`, `ghost`, `link`, `danger`, `icon`, состояниями `loading` и `disabled`.
- Введён `PpeModalShell`: портал, блокировка прокрутки, Escape/фон, удержание и возврат фокуса, закреплённые шапка и действия.
- На общий слой переведены мастер, каталог, модалки выдачи/возврата/списания, ключевые действия карточки и истории.
- Мобильный каталог сохраняет полноэкранный режим и не создаёт горизонтального переполнения.
- Экранные стили изолированы от печатного шаблона Word/PDF.
- Исправлены повреждённая подпись закрытия и разорванный идентификатор печатной строки.

Файлы: `PpeUi.tsx`, `ppe-ui-system.css`, активные экраны и модалки СИЗ, `ppeUi.test.tsx`.

Проверка: 6 из 6 точечных Vitest-тестов, `npm run typecheck` и production-сборка `npm run build`.

Ограничения: навигация, административный справочник норм, backend-контракты и печатный шаблон не изменялись.

## 2026-07-23 — исправление SQLCipher-транзакций Android

- Исправлено открытие транзакционных соединений локальной базы: каждое новое соединение теперь получает тот же SQLCipher-ключ до выполнения SQL.
- Транзакции выполняются через защищённый helper с `BEGIN IMMEDIATE`, корректным `COMMIT`/`ROLLBACK` и закрытием соединения.
- Все репозитории мобильного приложения переведены с `db.withExclusiveTransactionAsync` на защищённый путь.
- Добавлена точечная регрессионная проверка, запрещающая возврат старого незащищённого вызова.

Файлы: `mobiel proekt/src/db/encryptedDatabase.ts`, `mobiel proekt/src/db/database.ts`, `mobiel proekt/src/db/repositories/*`, `mobiel proekt/tests/databaseEncryptionPolicy.test.ts`.

Проверка: `npm run verify` — 58 тестов пройдено, encoding/typecheck/lint без ошибок; standalone debug APK собран и содержит SQLCipher native libraries.

Ограничение: физическое Android-устройство не подключено, поэтому установка и запуск APK через adb не выполнены.

## 2026-07-23 — production release APK

- Собран release APK версии `0.1.24` (`versionCode 25`) с текущим исправлением SQLCipher.
- APK подписан закреплённым release-сертификатом `dd9c7fa8…f345550`; подпись v2/v3 прошла проверку.
- В APK присутствуют `libexpo-sqlite.so` и `libcrypto.so` для `arm64-v8a`.

Файл: `mobiel proekt/build-output/patrol360-mobile-release.apk`.

Проверка: Gradle `assembleRelease` — успешно; размер APK около 61,7 MB.

Ограничение: запуск на физическом Android-устройстве не выполнен, так как `adb` недоступен в текущей среде.

## 2026-07-23 — исправление выбора контура мобильного сервера

- Убрано использование фиктивного production-домена `patrol360.example.com`.
- Release-скрипт по умолчанию выбирает `local-enterprise`, если контур явно не задан.
- Production теперь требует явный `PATROL360_PRODUCTION_API_URL`; случайная сборка с несуществующим хостом блокируется.
- Контур приложения остаётся связанным с сервером: локальный сервер использует `patrol360-local-enterprise`.

Файлы: `mobiel proekt/app.config.js`, `mobiel proekt/src/core/environments.ts`, `mobiel proekt/scripts/build-release-apk.ps1`.

Проверка: `npm run verify` — 58 тестов, encoding/typecheck/lint без ошибок. APK не пересобирался.

## 2026-07-23 — аудит мобильного приложения

- Проверены запуск приложения, SQLCipher-хранилище, восстановление сессии, refresh-токены, сетевые тайм-ауты, выбор контура и фоновая синхронизация.
- Подтверждена штатная работа локального API: 8 из 8 health-запросов успешны, контур `patrol360-local-enterprise`.
- Зафиксированы риски холодного запуска через экран офлайн-доступа, жёсткой привязки к LAN-IP, бесконечной загрузки при сбое SecureStore, частых `integrity_check` и конкурирующих записей SQLite.
- Создан отдельный отчёт с приоритетами и рекомендуемым порядком исправления.

Файл: `docs/mobile-app-audit-2026-07-23.md`.

Проверка: `npm run verify` — 58 тестов, encoding/typecheck/lint без ошибок; Docker-сервисы healthy.

Ограничение: подключённого Android-устройства или эмулятора нет, APK не собирался и не устанавливался.

## 2026-07-23 — резервный публичный адрес mobile API

- Основным адресом локального контура сохранён `http://192.168.2.194:5173`; адрес сетевого интерфейса компьютера подтверждён без изменений.
- Добавлен необязательный `PATROL360_PUBLIC_API_URL`, который включается вторым разрешённым адресом для `dev` и `local-enterprise`.
- HTTP-хост резервного адреса при необходимости включается в Android Network Security Config во время сборки; HTTPS не требует cleartext-исключения.
- Release- и APK-скрипты принимают параметр `-PublicApiBaseUrl`.
- Конкретный белый IP не записан, поскольку он отсутствует в проекте и не был передан пользователем.

Файлы: `mobiel proekt/app.config.js`, `mobiel proekt/src/core/environments.ts`, `mobiel proekt/scripts/build-release-apk.ps1`, `mobiel proekt/scripts/build-apk.ps1`, `mobiel proekt/README.md`.

Проверка: Expo-конфигурация загружена с тестовым адресом из зарезервированного диапазона; `npm run verify` — 58 тестов, encoding/typecheck/lint без ошибок; синтаксис PowerShell-скриптов проверен. APK не собирался.
## 2026-07-23 — исправление восстановления мобильной сессии и SQLite

- Действующая сохранённая сессия с access token после холодного запуска автоматически возвращает пользователя в рабочий экран.
- Офлайн-разблокировка остаётся только для сохранённой локальной сессии без access token.
- Добавлены понятные состояния повтора для ошибок SecureStore/локального профиля в стартовом маршруте, SessionGuard и экране офлайн-доступа.
- Убрано ложное сообщение о недоступности сервера с экрана офлайн-разблокировки.
- Полный SQLite `integrity_check` выполняется при обычном открытии/миграции, а не на каждом транзакционном соединении.
- Добавлен единый busy-retry для сохранения уведомлений и ключевых outbox-транзакций.
- Добавлена политика восстановления сессии и три точечных теста; общий набор вырос до 61 теста.

Файлы: `mobiel proekt/app/index.tsx`, `mobiel proekt/src/auth/SessionGate.tsx`, `mobiel proekt/app/(auth)/offline-login.tsx`, `mobiel proekt/src/auth/sessionRestorePolicy.ts`, `mobiel proekt/src/db/encryptedDatabase.ts`, `mobiel proekt/src/db/repositories/notificationRepository.ts`, `mobiel proekt/src/db/repositories/outboxRepository.ts`, `mobiel proekt/tests/sessionGatePolicy.test.ts`.

Проверка: `npm run verify` — 61 тест, encoding/typecheck/lint без ошибок. APK не собирался.
## Шаблон следующей записи

### YYYY-MM-DD — краткое название

#### Изменено

-

#### Файлы

-

#### Проверка

-

#### Ограничения

-

## 2026-07-23 — health probe cache and network diagnostics

- Added a short-lived per-server/per-contour health cache for ordinary API requests, token refresh and file uploads.
- Added in-flight request coalescing and cache invalidation after a transport failure; manual server checks remain fresh.
- Added typed network error causes (`timeout`/`network`) without changing the existing user-facing text.
- Added `tests/serverHealthCache.test.ts` and included it in `npm run verify`.

Verification: 64 tests, encoding check, TypeScript and lint passed. APK was not built.
## 2026-07-23 — резервный публичный IP и неприлипающий failover

- http://31.173.110.118 добавлен вторым кандидатом после локального API для dev/local-enterprise.
- Android Network Security Config разрешает этот HTTP-хост только в локальных контурах.
- Автоматический failover больше не записывает резервный адрес в SecureStore; локальный адрес остаётся предпочтительным.
- Ручная проверка в настройках сохраняет адрес только по явному действию пользователя.

Проверка: статический порядок кандидатов, отсутствие автоматического setServerBaseUrl в request/file failover, TypeScript и npm run verify. Резервный IP не подключён к серверу, поэтому его фактическая доступность не проверялась.

## 2026-07-23 — локальный сервер всегда первый в failover

- Добавлена чистая политика порядка кандидатов серверов.
- Для `dev` и `local-enterprise` локальный `http://192.168.2.194:5173` принудительно ставится первым перед сохранённым и резервным адресами.
- Тестовый и production-контуры не затронуты.

Проверка: `tests/serverCandidatePolicy.test.ts`; `npm run verify` — 65 тестов, encoding/typecheck/lint без ошибок.


## 2026-07-23 — сохранение причины сетевых ошибок вложений

- В `withTimeout` timeout теперь классифицируется отдельно.
- `fileApi` не теряет `timeout`/`network` при обработке ошибок загрузки.
- Пользовательские сообщения и контракты API не изменялись.

Проверка: `tests/networkTimeout.test.ts`; `npm run verify` — 66 тестов, encoding/typecheck/lint без ошибок.


## 2026-07-23 — отмена native upload при timeout

- Для фото и видео используется cancellable `FileSystem.createUploadTask`.
- Таймер вызывает `cancelAsync` перед последующей повторной отправкой.
- Добавлена проверка пустого результата после отмены.

Проверка: `tests/networkTimeout.test.ts`; `npm run verify` — 66 тестов, encoding/typecheck/lint без ошибок.

## 2026-07-23 — исправление пустого списка меток на экране обхода

- При локальном принятии заявки `patrol_assignments` теперь сразу получает `contour_id` текущего контура.
- Для уже созданных назначений без контура добавлено безопасное восстановление привязки перед загрузкой списка точек и карточки точки; назначение обновляется только для текущего владельца и только при `contour_id IS NULL`.
- Нормативный снимок точек, результаты обхода и очередь отправки не удаляются и не пересоздаются.
- Убран лишний параметр из запроса getAssignmentById, чтобы число аргументов SQLite соответствовало числу плейсхолдеров.

Файлы: `mobiel proekt/src/db/repositories/patrolRepository.ts`.

Проверка: `tests/patrolPointOwnerBinding.test.ts` — 1 тест пройден; `npm run typecheck` — успешно; статическая проверка SQL-привязки контура — успешно. APK не собирался, подключённое Android-устройство не использовалось.
## 23.07.2026 — Самостоятельный старт обхода и ранняя отправка вложений

- Серверный bootstrap теперь возвращает назначения в статусах `Assigned` и `Waiting` вместе с реальным `assignmentId`; запуск обхода с телефона больше не зависит от нажатия «Начать» в веб-интерфейсе.
- Свободная заявка на телефоне берётся в работу и открывается одним действием «Начать обход» через существующую атомарную команду `takePatrolRequest`.
- При bootstrap старый локальный идентификатор назначения сопоставляется с серверным без потери снимка маршрута, результатов точек, вложений и очереди команд; устаревшие конфликты `Assignment is not available` закрываются.
- Исправлена контурная фильтрация SQL при старте, паузе и продолжении обхода: количество параметров теперь соответствует запросам.
- Фото и видео начинают загружаться фоном после сохранения точки; финальная отправка отчёта сохраняет резервную проверку и догружает оставшиеся вложения.
- Завершённая заявка после подтверждения сервера остаётся терминальной и не должна повторно появляться как доступная из-за расхождения идентификаторов.
- APK не собирался.

## 23.07.2026 — отмена начатого обхода

- Серверная отмена переводит локальное назначение и заявку в `cancelledServer` независимо от того, пришла она через bootstrap или как ответ на команду очереди.
- Все неоконченные команды только этого обхода переводятся в `superseded`, а связанные конфликты закрываются; команды других обходов не затрагиваются.
- Результаты точек и локальные фото/видео не удаляются. Вложения отменённого обхода сохраняются для истории, но больше не показываются как ожидающие отправки и не блокируют локальные проверки данных.
- Отменённый обход исключается из активного сценария и не мешает начинать следующие обходы.

Проверка: `node --test --experimental-strip-types tests/patrolCancellationPolicy.test.ts` — 1 тест пройден; `npm run typecheck` — успешно. APK не собирался.

## 23.07.2026 — защита актуальной зашифрованной БД от отката

- При одновременном наличии зашифрованной и legacy-базы основной остаётся валидная зашифрованная БД со схемой приложения.
- Расхождение контрольных счётчиков больше не приводит к удалению зашифрованной БД и повторному импорту устаревших данных.
- Legacy-база удаляется только при полном совпадении счётчиков; при ошибке проверки она сохраняется как резервная копия и не блокирует запуск приложения.
- Повторная миграция выполняется только если существующая зашифрованная БД не содержит схемы приложения.

Проверка: `node --test --experimental-strip-types tests/databaseEncryptionPolicy.test.ts` — 6 тестов пройдено; `npm run typecheck` — успешно. APK не собирался.

## 23.07.2026 — исправление обновления рабочих заданий

- Удалён лишний `currentContourId` из параметров SQL-очистки синхронизированных рабочих заданий.
- Количество значений теперь соответствует плейсхолдерам: владелец и идентификаторы элементов, возвращённых сервером.
- При непустом ответе сервера локальный список больше не должен откатываться к старым данным из-за ошибки привязки SQLite.

Проверка: `node --test --experimental-strip-types tests/safetyPolicies.test.ts` — 5 тестов пройдено; `npm run typecheck` — успешно. APK не собирался.

## 23.07.2026 — оптимизация подготовки видео

- Видео больше не читается целиком в Base64 и не преобразуется в массив байтов на телефоне.
- Локальный SHA-256 сохраняется обязательным для фотографий; для видео хеш потоково вычисляет и сохраняет сервер.
- Идемпотентность повторной загрузки по стабильному `clientFileId` и проверка фактического размера сохранены.
- Параметр `sha256` загрузочного endpoint сделан nullable обратно совместимым способом.

Проверка: `node --test --experimental-strip-types tests/fileHash.test.ts` — 4 теста пройдено; `npm run typecheck` — успешно; `dotnet build apps/api/Patrol360.Api.csproj --no-restore --nologo` — успешно, 0 предупреждений и 0 ошибок. APK не собирался.

## 23.07.2026 — сохранение незавершённой точки обхода

- Выбранный статус, комментарий, тип неисправности и идентификаторы вложений автоматически сохраняются в локальный черновик через 400 мс после изменения.
- Черновик принудительно записывается при уходе приложения в фон, потере фокуса поля и перед открытием камеры, видео или галереи.
- Локальный черновик не создаёт команду outbox и не считается завершённой точкой; финальное сохранение по-прежнему выполняется отдельной атомарной операцией.
- Записи черновика сериализованы, поэтому более старое сохранение не может перезаписать новые данные или финальный результат.
- Состояние «Метка недоступна» корректно восстанавливается после перезапуска вместе с причиной и вложениями.

Проверка: `node --test --experimental-strip-types tests/safetyPolicies.test.ts` — 6 тестов пройдено; `npm run typecheck` — успешно. APK не собирался.
## 23.07.2026 — единый сеанс чтения NFC

- Перебор `Ndef`, `NfcA`, `NfcB`, `NfcV` и `IsoDep` заменён одним вызовом `requestTechnology` со списком поддерживаемых технологий.
- Android выбирает подходящую технологию для уже приложенной метки внутри одного сеанса; сотруднику больше не требуется удерживать или прикладывать метку повторно после неудачного `Ndef`.
- Удалены промежуточные отмены и повторные регистрации NFC-сеанса.
- После чтения выполняется одна быстрая очистка технологического запроса без дополнительной секундной задержки Android.

Проверка: `node --test --experimental-strip-types tests/nfcReadStrategy.test.ts` — 1 тест пройден; `npm run typecheck` — успешно. APK не собирался, проверка на физической NFC-метке не выполнялась.
## 23.07.2026 — защита NFC от повторного запуска

- В обработчик сканирования добавлен синхронный `scanInProgressRef`, который устанавливается до первого асинхронного вызова.
- Быстрое повторное нажатие и одновременный автоматический запуск больше не могут создать второй native-запрос NFC.
- Блокировка освобождается в `finally` при успешном чтении, отмене, выключенном NFC и ошибке, поэтому штатный повтор остаётся доступным.
- Существующие статусы экрана и пользовательские сообщения не изменялись.

Проверка: `node --test --experimental-strip-types tests/nfcScanGuard.test.ts` — 1 тест пройден; `npm run typecheck` — успешно. APK не собирался.
## 23.07.2026 — идемпотентный запуск плановой работы

- Запуск плановой работы теперь внутри существующей `BEGIN IMMEDIATE`-транзакции проверяет локальную `workSession` по владельцу и `planTaskId`.
- Повторное или двойное нажатие возвращает стабильный существующий `taskId` и не создаёт вторую строку либо outbox-команду.
- Исходная строка `planTask` больше не удаляется оптимистически; возможность повторного запуска блокируется через `capabilities_json`.
- Плановая строка удаляется только после ответа сервера `accepted` или `duplicate` для `startPlannedWork`.
- При `conflict` или `rejected` исходная плановая запись и локальная рабочая сессия сохраняются, поэтому данные не теряются до разрешения конфликта.

Проверка: `node --test --experimental-strip-types tests/plannedWorkStartSafety.test.ts` — 1 тест пройден; `npm run typecheck` — успешно. APK не собирался.
## 23.07.2026 — сохранение типа сетевой ошибки

- HTTP-клиент больше не заменяет `MobileNetworkError` обычным `Error` при запросе и после перебора резервных адресов.
- Разделены причины `timeout`, `offline`, `cancelled` и общая `network`; внешний `AbortSignal` больше не определяется как тайм-аут.
- При подтверждённом отсутствии сети очередь переводится в `waiting_network` без таймера повтора; тайм-аут и недоступный сервер остаются в управляемом `retryLater`.
- Пользовательские сообщения теперь указывают фактическую причину, а проверенные адреса сохраняются в контексте ошибки.

Проверка: `node --test --experimental-strip-types tests/networkTimeout.test.ts`; `npm run typecheck`. APK не собирался.
## 23.07.2026 — повтор служебных SQLite-записей при конкуренции

- Одиночные переходы статусов файлов и outbox-команд обёрнуты существующим `withSqliteBusyRetry`.
- Та же защита добавлена для локальной отметки уведомлений, сохранения push-токена и очереди отложенного выхода.
- UUID и время записи отложенного выхода вычисляются до retry, поэтому повтор использует тот же идентификатор и не создаёт вторую логическую операцию.
- SQL, допустимые переходы статусов, идентификаторы операций и транзакционные массовые обновления не изменялись.
- Добавлена целевая проверка, запрещающая прямые `await db.runAsync(...)` в этих четырёх служебных repository-файлах.

Проверка: `node --test --experimental-strip-types tests/sqliteServiceWriteRetry.test.ts`; `npm run typecheck`. APK не собирался.

## 23.07.2026 — устранение дефектов повторной синхронизации мобильных работ

- Авторизация, фоновое обновление и рабочий экран переведены на единый источник `/api/v2/mobile/work-items`; старый v1-путь больше не перезаписывает общую таблицу в активном сценарии.
- Пустой серверный список теперь удаляет устаревшие синхронизированные работы, сохраняя локальные строки со статусом `pending`.
- Пауза, продолжение и завершение работы проверяют существующую активную outbox-команду внутри `BEGIN IMMEDIATE`-транзакции и не создают дубль при быстром повторном нажатии.
- Ошибка загрузки вложения сохраняет тип `offline`, `timeout`, `cancelled` или `network` до Sync Engine.
- Маршруты и снимки точек защищены для всех незавершённых статусов обхода, включая `accepted`, `paused`, `syncError` и `authRequired`.
- Ошибки выполнения и регистрации фоновых задач записываются в мобильный диагностический журнал.

Проверка: `node --test --experimental-strip-types tests/mobileAuditRegression.test.ts` — 3 теста пройдено; `npm run typecheck` — успешно; `npm run check:encoding` — успешно. APK не собирался.
## 23.07.2026 — рефакторинг дашборда обходов

- Состояние, локальная загрузка, подписка на синхронизацию и ручное обновление вынесены из `PatrolHomeScreen` в `usePatrolHomeDashboard`.
- Ошибка чтения локальной базы больше не маскируется состоянием «Заявка не выбрана» и записывается в мобильную диагностику.
- Добавлена синхронная защита ручного обновления от повторного нажатия.
- Активный обход, пустое состояние, показатели и список заявок разделены на небольшие компоненты представления.
- Заголовки карточек допускают перенос длинного маршрута и статуса; области нажатия быстрых действий и обновления увеличены до 44 px.
- Локальные синие заливки заменены цветами текущей темы, добавлены состояния нажатия и accessibility-описания.
- Автоматическая проверка подтвердила, что все 35 локальных style-key используются; удалять подтверждённо неиспользуемые стили не потребовалось.

Проверка: `node --test --experimental-strip-types tests/patrolHomeViewModel.test.ts` — 1 тест пройден; `npm run typecheck` — успешно; точечный ESLint — успешно; `npm run check:encoding` — успешно. APK не собирался.
## 23.07.2026 — рефакторинг экрана «Результаты обходов»

- Строка результата вынесена из монолитного `ResultsWorkspace` в отдельный компонент `ResultsListRow`.
- Кликабельная область строки заменена на семантическую кнопку: выбор и двойное открытие доступны с клавиатуры, а меню действий больше не вложено в псевдокнопку.
- Верхние действия «Открыть заявку» и «Создать заявку» блокируются, когда текущий фильтр не содержит выбранного обхода; скрытый результат больше не используется неявно.
- В панель фильтров добавлены явное обновление, состояние загрузки и счётчик показанных результатов.
- Добавлен изолированный адаптивный слой `resultsWorkspace.css`: рабочая строка уплотнена на ноутбуках и переходит в карточное представление на узких экранах; используются общие `--ui-*` токены.
- Существующая загрузка данных, API, экспорт, просмотр вложений, контекстное меню и навигация не изменялись.

Проверка: `npm run test:unit -- --run src/__tests__/ui.test.tsx -t "does not keep header actions bound to a result hidden by the current filter"` — 1 тест пройден; `npm run typecheck` и `npm run build` — успешно. Визуальная проверка в браузере не выполнялась.
## 23.07.2026 — рефакторинг экрана «Назначение обходов»

- Поиск по сотрудникам больше не заменяет выбранного сотрудника первым совпадением: текущий выбор сохраняется, пока сотрудник существует в исходном справочнике.
- Создание заявки защищено синхронным single-flight флагом; быстрое повторное нажатие не запускает второй параллельный сценарий создания до React-перерисовки.
- Добавлена адаптивная панель текущего назначения с сотрудником, маршрутом, плановым временем, предупреждением о конфликте и основным действием создания.
- Панель использует общие `--ui-*` токены, перестраивается в одну колонку на узких экранах и сохраняет область основного действия не менее 44 px.
- Поисковому полю добавлена доступная подпись.
- Удалены подтверждённо неиспользуемые `hasStoredAssignmentFavoriteEmployeeIds`, `assignmentRequestIds` и локальный компонент `InputField`.
- API, контракты заявок, статусы, завершение, отмена и история назначений не изменялись.

Проверка: `npm run test:unit -- --run src/__tests__/domain.test.ts -t "keeps the selected assignment employee when search hides that employee"` — 1 тест пройден; `npm run typecheck` и `npm run build` — успешно. Визуальная проверка в браузере не выполнялась.
## 23.07.2026 — рефакторинг экрана «Сотрудники обхода»

- Пустой ответ API больше не подменяется демонстрационным справочником сотрудников; fallback используется только в локальном mock-режиме.
- Действия «Убрать из обхода» и «Деактивировать в справочнике» разделены и вызывают разные обработчики.
- Выбор сотрудников из общего справочника переведён на локальный черновик и сохраняется одной операцией; закрытие модалки отменяет несохранённые изменения.
- Сохранение рабочего списка защищено синхронным single-flight флагом, блокирует повторные действия и обновляет локальное хранилище только после успешного ответа API.
- Убрана фиктивная пагинация; добавлены сводка, сброс фильтров, доступные подписи и понятные пустые состояния.
- Добавлен изолированный адаптивный слой: полноэкранный выбор на телефоне, области действий не менее 44 px, устойчивое отображение длинных ФИО и должностей.
- Навигация, контракт сотрудников, структура базы и серверные методы не изменялись.

Проверка: `npm run test:unit -- --run src/__tests__/ui.test.tsx -t "applies patrol employee selection only after explicit save"` — 1 тест пройден; `npm run typecheck` и `npm run build` — успешно. Визуальная проверка в браузере не выполнялась.

## 23.07.2026 — стабилизация экрана «Мобильные аккаунты»

- Параллельные обновления списка защищены номером запроса: поздний устаревший ответ больше не перезаписывает более свежие данные.
- При временной ошибке API последний успешно загруженный список остаётся доступен; вместо полной замены экрана показывается компактное предупреждение с повтором.
- Во время сохранения блокируются смена выбранного аккаунта, вкладок и действия строки, чтобы операция не завершалась для другого аккаунта.
- Предупреждение адаптировано для телефона: действие имеет область нажатия 44 px и не вызывает горизонтального переполнения.
- API-контракты, авторизация, серверная модель аккаунта и мобильное приложение не изменялись.

Проверка: `npm run test:unit -- --run src/__tests__/ui.test.tsx -t "keeps the last mobile account list visible when refresh fails"` — 1 тест пройден; `npm run build` — TypeScript и production-сборка успешно. Визуальная проверка в браузере не выполнялась.

## 23.07.2026 — точечная стабилизация мастера выдачи СИЗ

- Сохранение реквизитов, сопоставления номенклатуры, добавления и удаления дополнительных строк, применения наборов и финальной выдачи защищены синхронным single-flight guard. Быстрое повторное нажатие не запускает вторую параллельную операцию.
- Восстановление черновика из localStorage теперь проверяет структуру документа, типы строк, положительное количество и цену; повреждённый или устаревший кэш игнорируется без подмены рабочего состояния.
- На этапе подбора кнопки добавления и массового выбора отключаются во время записи.
- Цена в модалке номенклатуры проверяется до отправки: допускается пустое значение либо конечное число не меньше нуля.
- Существующее разделение «норма → номенклатура → факт выдачи», API, печать и экспорт PDF/DOCX сохранены.

Проверка: `npm run test:unit -- --run src/__tests__/ppeIssueWorkflow.test.ts -t "malformed persisted draft"` — 1 тест пройден; `npm run test:unit -- --run src/__tests__/ppeIssueWorkflow.component.test.tsx -t "runs employee"` — 1 тест пройден; `npm run build` — TypeScript и production-сборка успешно. Визуальная проверка в браузере и APK не выполнялись.
## 23.07.2026 — унификация модалки и визуального слоя мастера СИЗ

- Модалка сопоставления номенклатуры получила синхронную защиту сохранения; повторный клик не запускает вторую запись.
- Во время сохранения заблокированы отмена, выбор другой позиции и повторные действия в списке каталога; ошибка не теряется из-за закрытия окна.
- Убраны градиенты шапки мастера и каталожного блока; поверхности и границы переведены на существующие `--ppe-ui-*` токены.
- Печатный шаблон, API, навигация и рабочая логика нормы/номенклатуры/факта не менялись.

Проверка: `npm run test:unit -- --run src/__tests__/ppeUi.test.tsx` — 2 теста пройдены; компонентный сценарий мастера — 1 тест пройден; `npm run typecheck` и `npm run build` — успешно. Визуальный прогон в браузере и APK не выполнялись.
## 23.07.2026 — защита сценария наборов СИЗ

- Загрузка состава набора и его применение защищены синхронным single-flight состоянием.
- Повторное нажатие не создаёт параллельную загрузку или повторную запись набора.
- Закрытие предпросмотра набора блокируется до завершения применения.
- Разделение норм, номенклатуры и факта выдачи не изменялось.

Проверка: `npm run test:unit -- --run src/__tests__/ppeIssueWorkflow.test.ts -t "applies a set without duplicate products"` — 1 тест пройден; компонентный сценарий мастера — 1 тест пройден; `npm run typecheck` и `npm run build` — успешно. Визуальная проверка браузера и APK не выполнялись.
## 23.07.2026 — унификация предпросмотра печати СИЗ

- Предпросмотр «Личная карточка / Лист подписи» переведён на общий `PpeModalShell`.
- Добавлены единые поведение Escape, закрытие по фону, возврат фокуса и блокировка прокрутки страницы.
- Печатное действие переведено на `PpeButton`; лист и его десятиколоночная печатная форма не изменялись.
- Тело предпросмотра разделено на переключатель режима и отдельную прокручиваемую область, включая мобильный полноэкранный режим.

Проверка: `npm run test:unit -- --run src/__tests__/ppeUi.test.tsx src/__tests__/ppePrintMapping.test.ts -t "PPE UI primitives|prints actual issued PPE"` — 3 теста пройдено; `npm run typecheck` и `npm run build` — успешно. Визуальный прогон браузера и APK не выполнялись.

## 24.07.2026 — экспорт PDF/DOCX в карточке СИЗ и доступная навигация

- На вкладке печати карточки сотрудника добавлены отдельные действия PDF и DOCX для «Личной карточки СИЗ» и «Листа подписи» через существующий `printPpeCard`.
- Экспорт защищён от повторных кликов: пока формируется один файл, остальные действия блокируются и показывают состояние загрузки; ошибка формирования возвращается пользователю через существующее уведомление.
- На мобильной ширине кнопки предпросмотра, PDF и DOCX перестраиваются без горизонтального переполнения.
- У навигации разделов СИЗ устранена конфликтующая ARIA-семантика tablist: маршруты и обработчики не менялись, элемент снова определяется как обычная навигация.
- Печатные шаблоны, API-контракт, сервер и мобильное приложение не изменялись.

Проверка: точечный сценарий `npm run test:unit -- --run src/__tests__/ui.test.tsx -t "opens PPE as an employee-centered card with tabs and issue action"` — 1 тест пройден, включая вызов PDF-экспорта; `npm run typecheck` и `npm run build` — успешно. Визуальная проверка браузера не выполнялась.
## 24.07.2026 — защита экспорта в мастере выдачи СИЗ

- На шаге «Печать и предпросмотр» PDF/DOCX теперь используют единое состояние операции: повторный клик не запускает второй запрос.
- Активный формат показывает загрузку, второй формат временно блокируется; ошибка экспорта остаётся в рабочем мастере и не закрывает сценарий.
- API `printPpeCard`, печатный шаблон и финальное атомарное сохранение не изменялись.

Проверка: `npm run test:unit -- --run src/__tests__/ppeIssueWorkflow.component.test.tsx -t "runs employee"` — 1 тест пройден, включая вызов PDF-экспорта; `npm run typecheck` — успешно. Визуальная проверка браузера и APK не выполнялись.